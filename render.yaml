services:
  - type: web
    name: fitfuel
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_RAPIDAPI_KEY
        sync: false
